export class Listado{

    // importe: string = '';
    // descripcion: string = '';

    // constructor(importe:string, descripcion:string){
    //     this.importe = importe;
    //     this.descripcion = descripcion;
    // }

    // forma simplificada declarar el atributo e inicializarlo
    constructor(public importe: number, public descripcion: string){}
}
